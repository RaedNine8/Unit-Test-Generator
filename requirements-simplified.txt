# Core dependencies for the simplified cover agent
requests>=2.31.0
psycopg2-binary>=2.9.5
python-dotenv>=1.0.0
jinja2>=3.1.2
pyyaml>=6.0
dynaconf>=3.2.0
grep-ast>=0.3.0

# Optional dependencies for LSP support (can be removed if not using)
tree-sitter>=0.20.0
tree-sitter-python>=0.20.0
tree-sitter-javascript>=0.20.0

# Development dependencies (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
coverage>=7.2.0
